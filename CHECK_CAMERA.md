# Проверка подключения к IP камере

## Проблема: "No route to host" (Нет маршрута к хосту)

Это означает, что камера **недоступна по сети**. Возможные причины:

### 1. Неправильный IP адрес камеры

Проверьте IP адрес камеры:

1. **Зайдите в веб-интерфейс камеры**:
   - Обычно через браузер: `http://192.168.0.200`
   - Если не открывается — IP адрес неправильный

2. **Найдите камеру в сети**:
   ```bash
   # macOS/Linux: найти все устройства в сети
   arp -a | grep 192.168.0
   
   # Или используйте приложение для поиска IP камер:
   # - Config Tool (Dahua)
   # - Advanced IP Scanner (Windows)
   # - Angry IP Scanner (Mac/Linux/Windows)
   ```

3. **Проверьте настройки роутера**:
   - Зайдите в роутер (обычно `192.168.0.1` или `192.168.1.1`)
   - Найдите "DHCP Clients" или "Connected Devices"
   - Найдите камеру Dahua в списке подключенных устройств

### 2. Камера в другой подсети

**Проблема:** Ваш компьютер и камера могут быть в разных подсетях.

**Проверка:**

```bash
# Проверьте IP вашего компьютера
ifconfig | grep "inet "

# Пример вывода:
# inet 192.168.1.100  <- ваш компьютер в сети 192.168.1.x
# inet 127.0.0.1      <- localhost
```

Если ваш компьютер в сети `192.168.1.x`, а камера в `192.168.0.x` — **они не видят друг друга!**

**Решение:**

**Вариант A:** Измените IP камеры (через Config Tool или веб-интерфейс):
- Новый IP: `192.168.1.200` (та же подсеть, что и компьютер)
- Маска: `255.255.255.0`
- Шлюз: `192.168.1.1`

**Вариант B:** Измените IP компьютера:
- Подключите компьютер к той же сети, где камера

### 3. Камера выключена или не подключена к сети

**Проверка:**

1. Убедитесь, что камера **включена** (горит индикатор питания)
2. Проверьте **Ethernet кабель**:
   - Кабель подключен к роутеру/свичу
   - Горят индикаторы на разъеме Ethernet (оранжевый/зеленый)

3. Попробуйте подключить камеру **напрямую к компьютеру**:
   - Ethernet кабель: камера → компьютер (без роутера)
   - Настройте статический IP на компьютере: `192.168.0.10`
   - Попробуйте пинговать камеру: `ping 192.168.0.200`

### 4. Файрволл блокирует подключение

**macOS:**

```bash
# Проверьте статус файрволла
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --getglobalstate

# Если включен, временно отключите для теста
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --setglobalstate off

# Попробуйте подключиться к камере

# Включите обратно
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --setglobalstate on
```

**Или через GUI:**
1. System Preferences → Security & Privacy → Firewall
2. Временно выключите файрволл для теста
3. Попробуйте подключиться к камере

### 5. VPN/Proxy блокирует локальную сеть

Если вы используете VPN:
1. **Отключите VPN**
2. Попробуйте подключиться к камере
3. Если заработало — настройте VPN для пропуска локальных адресов

## Пошаговая диагностика

### Шаг 1: Проверьте IP адрес вашего компьютера

```bash
ifconfig | grep "inet " | grep -v 127.0.0.1
```

Пример вывода:
```
inet 192.168.1.100 netmask 0xffffff00 broadcast 192.168.1.255
```

Ваша подсеть: `192.168.1.x`

### Шаг 2: Проверьте, что камера в той же подсети

Если компьютер в `192.168.1.x`, то камера должна быть в `192.168.1.x`.

**Текущий IP камеры в `.env`:**
```bash
cat .env | grep DAHUA_IP
# Вывод: PC_DAHUA_IP=192.168.0.200
```

Если камера в другой подсети — **измените IP!**

### Шаг 3: Найдите камеру через Config Tool

**Dahua Config Tool** (официальное приложение):

1. Скачайте: [Dahua Downloads](https://www.dahuasecurity.com/support/downloadCenter)
2. Запустите **ConfigTool**
3. Нажмите **"Device Search"**
4. Найдите вашу камеру в списке — увидите **реальный IP адрес**

### Шаг 4: Измените IP в `.env`

Откройте `.env` и укажите **правильный IP**:

```bash
PC_DAHUA_IP=192.168.1.200  # Ваш реальный IP камеры
```

Перезапустите приложение:

```bash
pkill -9 -f "run.py"
.venv/bin/python run.py --no-debug-window
```

### Шаг 5: Попробуйте снова в браузере

1. http://localhost:8000
2. Выберите **"🎥 IP камера Dahua"**
3. Дождитесь результата

## Быстрый тест RTSP

Проверьте RTSP поток через VLC:

1. Откройте **VLC Media Player**
2. `Media` → `Open Network Stream`
3. Вставьте URL:
   ```
   rtsp://admin:daniil2009@192.168.0.200:554/cam/realmonitor?channel=1&subtype=0
   ```
4. Нажмите `Play`

**Если видео играется в VLC** — значит:
- ✅ IP адрес правильный
- ✅ Пароль правильный
- ✅ RTSP работает

**Если не играется:**
- ❌ Проверьте IP адрес (см. выше)
- ❌ Проверьте пароль
- ❌ Проверьте, включен ли RTSP на камере

## Настройка RTSP на камере Dahua

1. Зайдите в веб-интерфейс камеры: `http://192.168.0.200`
2. Логин: `admin`, Пароль: `daniil2009`
3. `Setup` → `Network` → `Port`
4. Убедитесь:
   - **RTSP Port = 554** ✅
   - **Enable RTSP** ✅
5. Сохраните и перезагрузите камеру

## Использование sub stream (меньше нагрузка)

Если main stream не работает, попробуйте sub stream:

В `.env`:
```bash
PC_DAHUA_SUBTYPE=1  # 1 = sub stream (SD, ~360p)
```

RTSP URL для VLC:
```
rtsp://admin:daniil2009@192.168.0.200:554/cam/realmonitor?channel=1&subtype=1
```

---

## Итоговый checklist

- [ ] Проверили IP компьютера (`ifconfig`)
- [ ] Проверили IP камеры (Config Tool или веб-интерфейс)
- [ ] Камера и компьютер в одной подсети (192.168.X.X)
- [ ] Камера пингуется (`ping IP_камеры`)
- [ ] RTSP работает в VLC
- [ ] IP правильный в `.env`
- [ ] Пароль правильный в `.env`
- [ ] Файрволл/VPN не блокирует
- [ ] Приложение перезапущено после изменений

---

**После исправления попробуйте снова!** 🚀
