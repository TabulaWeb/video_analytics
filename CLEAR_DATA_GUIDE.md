# Руководство по очистке данных 🧹

## Обзор

В системе People Counter есть три типа данных, которые можно очистить:

1. **Счетчики IN/OUT** - текущие значения счетчиков (временные, в памяти)
2. **Recent Events** - история событий пересечения линии (SQLite база данных)
3. **Known Persons** - база известных людей Re-ID (pickle файл)

---

## 1. Reset Counters (Сброс счетчиков) 🔄

### Что очищается:
- ✅ Счетчики IN и OUT → 0
- ✅ Активные треки в памяти
- ✅ Состояние подсчета для каждого person_id

### Что НЕ очищается:
- ❌ События в базе данных (сохраняются)
- ❌ Re-ID база известных людей (сохраняется)

### Через Web UI:

1. Откройте http://0.0.0.0:8000
2. Нажмите кнопку **"🔄 Reset Counters"**
3. Подтвердите действие

**Результат:**
```
IN: 0  OUT: 0
Active tracks: 0
```

### Через API:

```bash
curl -X POST http://localhost:8000/api/reset
```

**Ответ:**
```json
{
  "success": true,
  "message": "Counters reset successfully (Re-ID database preserved)",
  "new_stats": {
    "in_count": 0,
    "out_count": 0,
    "active_tracks": 0
  }
}
```

### Когда использовать:
- ✅ Начало нового рабочего дня
- ✅ После тестирования
- ✅ Смена смены / периода учета

---

## 2. Clear Events (Очистка событий) 🗑️

### Что очищается:
- ✅ Вся история событий из базы данных
- ✅ Все записи IN/OUT с временными метками

### Что НЕ очищается:
- ❌ Текущие счетчики (остаются как есть)
- ❌ Re-ID база известных людей (сохраняется)

### Через Web UI:

1. Откройте http://0.0.0.0:8000
2. Нажмите кнопку **"🗑️ Clear Events"**
3. Подтвердите действие (⚠️ НЕОБРАТИМО!)

**Результат:**
```
Recent Events:
(пусто - все события удалены)
```

### Через API:

```bash
curl -X POST http://localhost:8000/api/events/clear
```

**Ответ:**
```json
{
  "success": true,
  "message": "All events cleared successfully"
}
```

### Когда использовать:
- ⚠️ База данных слишком большая
- ⚠️ Нужно очистить старые данные
- ⚠️ Миграция / переустановка системы

**⚠️ ВНИМАНИЕ:** Это действие необратимо! Все события будут удалены из базы данных.

---

## 3. Clear Re-ID Database (Очистка базы Re-ID) 🧠

### Что очищается:
- ✅ Все известные person_id (P0001, P0002, ...)
- ✅ Все embeddings и similarity данные
- ✅ История появлений каждого человека

### Что НЕ очищается:
- ❌ Текущие счетчики IN/OUT
- ❌ События в базе данных

### Через Web UI:

1. Откройте http://0.0.0.0:8000
2. Нажмите **"👤 Known Persons"**
3. Нажмите **"🗑️ Clear All"**
4. Подтвердите действие (⚠️ НЕОБРАТИМО!)

**Результат:**
```
Known Persons: 0
(все person_id удалены, система начнет регистрировать людей заново)
```

### Через API:

```bash
curl -X POST http://localhost:8000/api/reid/clear
```

**Ответ:**
```json
{
  "success": true,
  "message": "Re-ID database cleared successfully"
}
```

### Когда использовать:
- ⚠️ Два разных человека определяются как один (порча базы)
- ⚠️ Слишком много ложных совпадений
- ⚠️ Смена контингента людей (например, новые сотрудники)
- ⚠️ Тестирование Re-ID с чистого листа

**⚠️ ВНИМАНИЕ:** После очистки Re-ID все люди будут определяться как новые!

---

## 4. Cleanup Old Persons (Удаление старых записей) 🧹

### Что удаляется:
- ✅ Person_id которые не видели больше N дней
- ✅ Их embeddings и метаданные

### Что НЕ удаляется:
- ❌ Недавно виденные люди
- ❌ События и счетчики

### Через API:

```bash
# Удалить людей не виденных 7+ дней
curl -X POST "http://localhost:8000/api/reid/cleanup?max_age_days=7"
```

**Ответ:**
```json
{
  "success": true,
  "removed_count": 3,
  "message": "Removed 3 persons not seen in 7 days"
}
```

### Когда использовать:
- ✅ Регулярная очистка (cron job)
- ✅ База Re-ID растет слишком быстро
- ✅ Нужно освободить место

**Рекомендация:** Настроить автоматическую очистку раз в неделю:
```bash
# Crontab
0 0 * * 0 curl -X POST "http://localhost:8000/api/reid/cleanup?max_age_days=7"
```

---

## Сравнение операций

| Операция | Счетчики | События (DB) | Re-ID база | Обратимо |
|----------|----------|--------------|------------|----------|
| **Reset Counters** | ✅ Очищает | ❌ Сохраняет | ❌ Сохраняет | ✅ Да* |
| **Clear Events** | ❌ Сохраняет | ✅ Удаляет | ❌ Сохраняет | ❌ Нет |
| **Clear Re-ID** | ❌ Сохраняет | ❌ Сохраняет | ✅ Удаляет | ❌ Нет |
| **Cleanup Old** | ❌ Сохраняет | ❌ Сохраняет | ⚠️ Частично | ❌ Нет |

*Счетчики можно восстановить из событий в базе данных

---

## Типичные сценарии

### Сценарий 1: Начало нового дня

```bash
# Сбросить только счетчики
curl -X POST http://localhost:8000/api/reset
```

**Результат:**
- IN/OUT → 0
- События вчерашнего дня сохранены в БД
- Re-ID помнит всех известных людей

### Сценарий 2: Полная очистка (новый старт)

```bash
# 1. Очистить события
curl -X POST http://localhost:8000/api/events/clear

# 2. Очистить Re-ID
curl -X POST http://localhost:8000/api/reid/clear

# 3. Сбросить счетчики
curl -X POST http://localhost:8000/api/reset
```

**Результат:**
- Система как будто только что установлена
- Все данные удалены

### Сценарий 3: Проблема с Re-ID (ложные срабатывания)

```bash
# Очистить только Re-ID, сохранив события
curl -X POST http://localhost:8000/api/reid/clear
```

**Результат:**
- События пересечения линии сохранены
- Счетчики сохранены
- Re-ID начнет регистрировать людей заново

### Сценарий 4: Еженедельная очистка старых записей

```bash
# Удалить людей не виденных неделю
curl -X POST "http://localhost:8000/api/reid/cleanup?max_age_days=7"
```

**Результат:**
- Недавние person_id сохранены
- Старые записи удалены
- База данных оптимизирована

---

## Web UI: Где найти кнопки

### Main Page (http://0.0.0.0:8000)

```
┌─────────────────────────────────────┐
│  Recent Events                      │
│  ┌───────────┬──────────┬────────┐ │
│  │🔄 Reset   │🗑️ Clear │👤 Known│ │
│  │ Counters  │ Events  │ Persons│ │
│  └───────────┴──────────┴────────┘ │
│                                     │
│  Event 1: IN  - 12:30:45           │
│  Event 2: OUT - 12:29:12           │
│  ...                                │
└─────────────────────────────────────┘
```

### Known Persons Panel

```
┌─────────────────────────────────────┐
│  Known Persons (Re-ID)              │
│  ┌────────┬──────────────────┐     │
│  │🔄 Refresh│🗑️ Clear All   │     │
│  └────────┴──────────────────┘     │
│                                     │
│  👤 P0001  Seen 15 times           │
│  👤 P0002  Seen 8 times            │
│  ...                                │
└─────────────────────────────────────┘
```

---

## Рекомендации

### Ежедневно:
```bash
# Утром - сброс счетчиков
curl -X POST http://localhost:8000/api/reset
```

### Еженедельно:
```bash
# Удаление старых Re-ID записей
curl -X POST "http://localhost:8000/api/reid/cleanup?max_age_days=7"
```

### Ежемесячно:
```bash
# Опционально - очистка старых событий если БД большая
# (или делайте бэкап и очищайте)
curl -X POST http://localhost:8000/api/events/clear
```

### При проблемах:
```bash
# Re-ID работает некорректно
curl -X POST http://localhost:8000/api/reid/clear

# Перезапуск с чистого листа
curl -X POST http://localhost:8000/api/events/clear
curl -X POST http://localhost:8000/api/reid/clear
curl -X POST http://localhost:8000/api/reset
```

---

## Backup перед очисткой

### Сохранить события:
```bash
# Экспорт событий в JSON
curl http://localhost:8000/api/events?limit=10000 > events_backup.json
```

### Сохранить Re-ID базу:
```bash
# Копировать файл базы
cp data/reid_db.pkl data/reid_db_backup_$(date +%Y%m%d).pkl
```

### Восстановление Re-ID:
```bash
# Восстановить из бэкапа
cp data/reid_db_backup_20260212.pkl data/reid_db.pkl

# Перезапустить приложение
```

---

## API Reference

### POST `/api/reset`
Сбросить счетчики IN/OUT
- **Body:** (нет)
- **Response:** `{success, message, new_stats}`

### POST `/api/events/clear`
Удалить все события из базы данных
- **Body:** (нет)
- **Response:** `{success, message}` или `{success, error}`

### POST `/api/reid/clear`
Удалить всех известных людей из Re-ID базы
- **Body:** (нет)
- **Response:** `{success, message}` или `{success, error}`

### POST `/api/reid/cleanup?max_age_days=7`
Удалить людей не виденных N дней
- **Query:** `max_age_days` (default: 7)
- **Response:** `{success, removed_count, message}`

---

## Troubleshooting

### Кнопка не работает

**Проверить консоль браузера:**
```javascript
// Открыть Developer Tools (F12)
// Console tab → смотреть ошибки
```

**Проверить API вручную:**
```bash
curl -v -X POST http://localhost:8000/api/events/clear
```

### База данных заблокирована

```bash
# Остановить приложение
pkill -9 -f "run.py"

# Проверить что файл не заблокирован
lsof | grep people_counter.db

# Перезапустить
python run.py --no-debug-window
```

### Re-ID не очищается

```bash
# Проверить файл существует
ls -la data/reid_db.pkl

# Удалить вручную если нужно
rm data/reid_db.pkl

# Перезапустить приложение
```

---

## Summary

✅ **Добавлено:**
- Кнопка "🗑️ Clear Events" в Recent Events секции
- Кнопка "🗑️ Clear All" в Known Persons панели
- API endpoint: `POST /api/events/clear`
- Подтверждение перед удалением (confirm dialogs)
- Цветовое кодирование кнопок (синий/красный)

🎨 **UI Улучшения:**
- 🔄 Reset Counters → синяя кнопка
- 🗑️ Clear Events → красная кнопка
- 👤 Known Persons → серая кнопка

⚠️ **Безопасность:**
- Все деструктивные операции требуют подтверждения
- Предупреждения о необратимости действий
- Сохранение связанных данных (Re-ID при очистке events)

---

**Готово к использованию!** Откройте http://0.0.0.0:8000 и протестируйте новые кнопки! 🎉
